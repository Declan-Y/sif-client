import LoginButton from "../login";
import LogoutButton from "../logout";

export const HomePage = () => {
  return (
    <div>
      <LoginButton />
      <LogoutButton />
      <h1>HomePage</h1>
    </div>
  );
};
